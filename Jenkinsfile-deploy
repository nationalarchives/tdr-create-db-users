library("tdr-jenkinslib")

deployToLambda(
        stage: params.STAGE,
        libraryName: "create-db-users",
        version: params.TO_DEPLOY,
        deploymentPackageName: "create-db-users.jar"
)
